{
  "name": "loja-online",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://seuusuario.github.io/seurepositorio",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "fix-paths": "react-scripts build && mv build/index.html . && mv build/static static && cp -r build/* .",
    "test": "react-scripts test"
  },
  "dependencies": {
    "firebase": "^9.22.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.2",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}